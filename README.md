# 🧾 Student Record Manager (Python)

The **Student Record Manager** is a simple Python-based command-line application that helps manage student details efficiently. It allows users to **add, view, search, update, and delete** student records, all stored persistently in a lightweight JSON file. This project is ideal for beginners learning Python file handling and CRUD operations.

---

## 🚀 Features

- **Add Student Record:** Store details like name, roll number, age, and marks.
- **View All Records:** Display all stored student data in a readable format.
- **Search Student:** Find a student easily by name or roll number.
- **Update Record:** Edit existing details while keeping other fields unchanged.
- **Delete Record:** Remove a student’s record safely with confirmation.
- **Persistent Storage:** All data is stored in `record.json` and reused across runs.
- **Interactive Menu:** Simple menu interface for user-friendly navigation.

---

## 🛠️ Technologies Used

- **Language:** Python  
- **Data Storage:** JSON  

---

## 💡 How to Run

1. Clone the repository:
   ```bash
   git clone https://github.com/b-shrutika/student-record-manager.git

2. Navigate to the project folder:
   ```bash
     cd student-record-manager
3. Run the program:
   ```bash
   python student_record_manager.py


